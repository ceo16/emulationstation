#ifndef ES_CORE_UTILS_RANDOMSTRING_H
#define ES_CORE_UTILS_RANDOMSTRING_H

#include <string>

namespace Utils {
    namespace String {
        std::string generateRandom(size_t length);
    }
}

#endif // ES_CORE_UTILS_RANDOMSTRING_H